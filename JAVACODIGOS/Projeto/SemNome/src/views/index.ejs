<%- include('includes/head') %>
<%- include('includes/nav') %>

<section id="content">
    <% if (erros.length !== 0) { %>
        <div class = "SystemMessageErro">
            <% erros.map((erro) => {%>
                <p><%= erro %></p>
            <% }) %>
        </div>
    <% } %>
    <div id = "container">
        <% produtos.forEach(produto => {%>
            <div id = "bloco" >
                <p>Nome: <span class="destaque"><%= produto.NomeProduto %></span></p>
                <p>Em estoque: <span class="destaque"><%= produto.Quantidadeproduto %></span></p>
                <p>Preço: <span class="destaque"><%= produto.preco %></span></p>
                <div id="opc">
                    <a class="btn-editar" href="/produto/indexEdit?id=<%= produto._id %>">Editar</a>
                    <a class="btn-diminuir" href="/contato/delete?id=<%= produto._id %>">Diminuir</a>
                </div>
            </div>
        <% }) %>
    </div>
</section>
<link rel="stylesheet" href="/assets/css/index.css">
<%- include('includes/rodape') %>
